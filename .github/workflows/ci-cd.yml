# name: CI/CD Pipeline

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest
#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v2

#     - name: Log in to Azure Container Registry
#       uses: azure/docker-login@v1
#       with:
#         login-server: telegramappenergyapi.azurecr.io
#         username: ${{ secrets.ACR_USERNAME }}
#         password: ${{ secrets.ACR_PASSWORD }}

#     - name: Build and push Docker image
#       run: |
#         docker build --platform linux/amd64 -t telegramappenergyapi.azurecr.io/mytelegrambot:${{ github.sha }} .
#         docker push telegramappenergyapi.azurecr.io/mytelegrambot:${{ github.sha }}

#     - name: Deploy to Azure Container Apps
#       uses: azure/webapps-deploy@v2
#       with:
#         app-name: mytelegrambot
#         slot-name: production
#         publish-profile: ${{ secrets.AZURE_PUBLISH_PROFILE }}
#         images: 'telegramappenergyapi.azurecr.io/mytelegrambot:${{ github.sha }}'
